# Copy to .env to override. This is not a secret: it ends up in the page as
# built. Without any .env at all, `npm run dev` links to the app on this
# machine and `npm run build` links to the published one.

# Where the app itself is. The only setting there is.
PUBLIC_APP=https://choai.dev
